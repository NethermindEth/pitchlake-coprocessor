# This is currently not working

Using fixed point, we are seeing that there is a division by zero error in our `minimize` function. This error is not present in our floating point implementation. 

To fix this if we need fixed point optimization in the other PRs